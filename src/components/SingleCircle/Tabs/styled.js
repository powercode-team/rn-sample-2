import styled from 'styled-components';

const Wrapper = styled.View`
  padding-vertical: 20;
`;

const TabsWrapper = styled.View`
  flex-direction: row;
  justify-content: center;
  align-items: center;
`;

const Content = styled.View``;

export { Wrapper, TabsWrapper, Content };
